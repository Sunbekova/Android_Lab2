<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Dogs_API" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/purple</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Цвет иконки поиска -->
        <item name="android:searchIcon">@drawable/ic_search_white</item>
        <!-- Цвет текста -->
        <item name="android:textColor">@android:color/white</item>
        <!-- Цвет курсора -->
        <item name="android:textCursorDrawable">@null</item>
    </style>

    <style name="Theme.Dogs_API" parent="Base.Theme.Dogs_API" />
</resources>